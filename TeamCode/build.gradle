apply plugin: 'com.android.application'

//
// build.gradle in TeamCode
//
// Most of the definitions for building your module reside in a common, shared
// file 'build.common.gradle'. Being factored in this way makes it easier to
// integrate updates to the FTC into your code. If you really need to customize
// the build definitions, you can place those customizations in this file, but
// please think carefully as to whether such customizations are really necessary
// before doing so.

// Custom definitions may go here

// Include common definitions from above.
apply from: '../build.common.gradle'
apply from: '../build.dependencies.gradle'

android {
    namespace = 'org.firstinspires.ftc.teamcode'

    packagingOptions {
        jniLibs.useLegacyPackaging true
    }
}

repositories {
    // FTC Dashboard isn't on Maven Central/Google — it's published here.
    maven {
        url = 'https://maven.brott.dev/'
    }
    // Panels isn't on Maven Central either — confirmed against the official
    // ftcontrol/Panels-Quickstart-Java repo's build.dependencies.gradle.
    maven {
        url = 'https://mymaven.bylazar.com/releases'
    }
}

dependencies {
    implementation project(':FtcRobotController')

    // FTC Dashboard — live telemetry graphs + on-the-fly tuning of @Config fields
    // (see TeleopDrive.java) without redeploying code.
    implementation "com.acmerobotics.dashboard:dashboard:0.5.1"

    // Panels (panels.bylazar.com) — a second, more full-featured dashboard: telemetry,
    // graphs, @Configurable live tuning, OpMode control, and more. Version confirmed
    // against the official quickstart repo's current build.dependencies.gradle.
    implementation "com.bylazar:fullpanels:1.0.13"
}
